# Google OAuth Configuration for YouTube Integration
# Get these values from Google Cloud Console: https://console.cloud.google.com/

# Your Google OAuth Client ID (public, safe to expose in frontend)
GOOGLE_CLIENT_ID=your_google_client_id_here

# Your Google OAuth Client Secret (server-only, keep secure)
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# The redirect URI for OAuth callback
# For development: http://localhost:3000/oauth-callback
# For production: https://your-domain.com/oauth-callback
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth-callback

# Note: Make sure to add these exact URLs to your Google OAuth configuration:
# - Authorized JavaScript origins: your site URL
# - Authorized redirect URIs: the GOOGLE_REDIRECT_URI value above